# Feature Products

This package provides a product catalog feature with search, filtering, and sorting.

## Features

- `ProductsFeature` renders a searchable, filterable list of products.
- `fetchProducts` simulates an API request for catalog data.
- Helpers for filtering and sorting product data.
