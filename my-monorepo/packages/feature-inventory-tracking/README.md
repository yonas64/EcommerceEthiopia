# feature-inventory-tracking

Inventory and availability UI + utilities.

Exports:
- `LowStockBadge` — displays a low-stock indicator
- `BackInStockForm` — subscribe for notifications
- `checkAvailability`, `reserveStock` — simple demo utilities

Notes: These are UI + small client-side helpers. Replace `checkAvailability`/`reserveStock` with real API calls or websocket hooks for production.
